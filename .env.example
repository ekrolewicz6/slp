# Restricted-dataset credentials (Phase 2+). Copy to .env and fill in.
# Single TalkBank Express session cookie value (login at aphasia.talkbank.org,
# copy from browser DevTools → Application → Cookies → talkbank or connect.sid).
# Both cookie names share the same value in current TalkBank deployment.
APHASIABANK_COOKIE=

RELEASE_TOKEN=
